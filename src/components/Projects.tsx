"use client";
import React from 'react';
import ListDetailSection from './ListDetailSection';

export default function Projects() {
    return (
        <ListDetailSection
            type="projects"
            title="Projects"
            description="Selected builds spanning ML, blockchain analytics, and full-stack systems. Choose a project to see the full context."
        />
    );
}
